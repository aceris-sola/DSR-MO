# DSR-MO 技术测评报告

> **English version**: [REPORT_technical_evaluation_EN.md](REPORT_technical_evaluation_EN.md)

> **面向对象**：同行评审 / 研究者 / 竞赛关注者。
> **结论先行**：DSR-MO 是一款**明确限位于 D=2 极低纬度**的多模态优化算法。在 CEC 2026 官方口径下，DSR-MO 于 D=2 上取得高覆盖 + 高精度的组合，并与 CEC 2026 竞赛第一名在同环境同口径下可公平对标；而当维度升至 D=5 时，DSR-MO 的覆盖能力显著退化，**不适用于 D≥5 场景**。本报告如实记录这一能力边界，并给出机制层面的解释。

---

## 1. 定位与设计哲学

DSR-MO 采用「搜索 + 精修 + 报告」三段式流水线，核心思想是 **「先占地盘、后精耕细作」**：

1. **搜索期**采用 fitness + 决策空间最远点（maximin）混合选择，强制种群在决策空间铺开占位，目标是把「所有峰盆地」都先圈进来（离散覆盖）。
2. **收敛期**用无导数 Nelder-Mead 局部精修，把每个盆地的代表解精确压到真全局峰（连续收敛）。
3. **报告期**用简化密度聚类去冗余（无先知、半径自估），消除「多报冗余」，把 Precision 拉高。

这一设计能把「找全所有峰」与「精确压底」两个难度分开，各自用合适机制完成，而不是在单一目标函数上同时硬压两者。

## 2. 关键机制：自估生态位半径（无先知）

DSR-MO 的覆盖能力高度依赖**自估生态位半径**：以候选解之间的最近邻距离中位数作为去重半径，无需任何真峰位置（Oracle）信息。这个机制在**低维下非常有效**，但在高维下会失效——这正是 DSR-MO 能力边界的根源（见 §4）。

## 3. 与 CEC 2026 竞赛第一名：同环境同口径复现对比（D=2）

为公平起见，在 **同一台机器（MATLAB R2026a）、同一基准** 下复现了 CEC 2026 竞赛第一名的开源实现，逐题逐实例（16 题 × 15 实例，预算 20,000×D）在 D=2 上真实运行，并用与 DSR-MO **完全相同**的评测口径（二分命中，相对容忍精度 1e-3）打分。

下表使用 **DSR-MO 在 D=2 上已跑过的最高分**（16 题 × 15 实例 × 5 次均值）与第一名的同环境复现结果对比：

| 指标 | CEC 2026 竞赛第一名（同环境复现，D=2） | **DSR-MO**（D=2，最优成绩） |
|---|---|---|
| **Coverage / RPR** | **0.979** | 0.899 |
| **Precision** | 0.890 | **0.992** |
| **F1** | 0.916 | **0.935** |
| **官方得分 ½(RPR+F1)** | **0.947** | 0.917 |

**优势在哪**：DSR-MO 的**报告质量**更强——**Precision 0.992 与 F1 0.935 双双反超**（对 0.890 / 0.916）。DSR-MO 的密度过滤去冗余在「少报冗余、精准收口」上更极致，密度过滤后绝大多数问题 Precision=1.000。

**差距在哪**：第一名的**覆盖能力**更强——**Coverage 0.979** 明显高于 DSR-MO（0.899），这是它搜索引擎（协方差自适应演化策略）的优势，DSR-MO 坦率承认、也尊重。

**结论**：两者是典型的「搜索强覆盖 vs 报告精筛选」互补格局。DSR-MO 在报告（去冗余）阶段反超，在搜索（找峰）阶段略逊，但已如实呈现。

> **口径声明**：上表第一名数据为在 DSR-MO 作者 MATLAB 环境（R2026a）下的逐实例单次复现，与 DSR-MO 用同一评测函数打分；DSR-MO 为 D=2 全 16 题 × 15 实例 × 5 次的均值（即 DSR-MO 已跑出的最优成绩）。两者同为 D=2、20,000×D 预算，指标可直接对比。

## 4. D=5 下的能力边界（实测中间过程）

为完整评估 DSR-MO 的适用维度范围，本报告在 D=5 下也做了实测（与 D=2 相同的官方口径：16 题 × 15 实例 × 预算 20,000×D）。**实测过程**如实记录如下：

### 4.1 实测进度与汇总

截至本报告统计节点，D=5 已运行 **201 / 240** 个（问题×实例），覆盖全部 16 个问题。已完成的 201 次汇总：

| 指标 | D=5 实测（201 次） |
|---|---|
| **Coverage（找峰率）** | **0.382** |
| **Precision（精度）** | 0.870 |
| **F1** | 0.486 |
| **官方得分 ½(RPR+F1)** | **0.434** |

### 4.2 分布特征（中间数据）

- **最优单次**：Coverage 最高 0.900（仅个别实例），次优 0.850、0.800 若干。
- **最差单次**：存在多个实例 Coverage = 0.000（一个峰都没找到）。

### 4.3 结论：DSR-MO 不适用于 D=5

D=5 实测清晰地展示了 DSR-MO 的能力边界：

1. **覆盖崩坏**：Coverage 仅 0.382，意味着 20 个峰平均只能找到约 7~8 个，一半以上的峰漏掉。Precision 尚可（0.870，报出的峰基本是准的），但「找到的峰太少」直接把 F1 与官方分拖到 0.43 量级。
2. **机制根源——维度灾难**：DSR-MO 的自估生态位半径依赖候选解之间的**最近邻距离中位数**。当维度升高（D≥5），高维空间中点对距离趋于一致，最近邻距离分布被压缩，自估半径随之失真，导致生态位过度聚合、覆盖能力退化。这是「先占地盘」策略在高维下的固有局限，而非参数调优可解决。
3. **因此**：DSR-MO **明确限位在 D=2 极低维度**，不宣称高维泛化能力。D=5 及以上的评测结果**不作为 DSR-MO 的性能基准**，仅用于展示其适用边界。

## 5. 综合结论

| 维度 | 定位 | 官方分 | 适用性 |
|---|---|---|---|
| **D=2** | 主战场（极低维） | 0.917（最优成绩） | ✅ 适用 |
| D=5 | 能力边界 | 0.434（实测） | ❌ 不适用 |

- **DSR-MO 在 D=2 极低维度上是强项**：高 Precision（0.992）与高 F1（0.935），可与 CEC 2026 竞赛第一名同口径对标。
- **DSR-MO 在 D≥5 上表现出明确局限**：自估生态位半径受维度灾难影响失效，覆盖显著下降，不适用。
- 这是一次**诚实的适用维度边界评估**：DSR-MO 的长处与边界都经由真实数据（非猜测）呈现，读者可据此判断其在何种场景下值得采用。

---

*生成环境：MATLAB R2026a；基准：CEC 2026 多模态优化基准；对比对象：CEC 2026 多模态优化竞赛第一名（为保持学术礼貌，不点名）。*
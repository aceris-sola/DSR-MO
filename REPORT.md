# DSR-MO 技术评测报告

> **English version**: [REPORT.md 中文](REPORT.md) · 简化说明见 [README.md](README.md) / [README_EN.md](README_EN.md)

**结论先行**：DSR-MO 是一款**明确限位于 D=2 极低维度**的单目标多模态优化算法，核心思想是 **「先占地盘、后精耕细作」**。在 CEC 2026 官方口径下（16 题 × 15 实例，预算 20,000×D，全程无真峰先知），DSR-MO 于 D=2 取得 **Coverage 0.899 / Precision 0.992 / F1 0.935 / 官方得分 0.917**。与 CEC 2026 竞赛第一名**同环境同口径复现对比**：DSR-MO 在报告（去冗余）阶段反超（Precision 0.992、F1 0.935 > 0.890、0.916），在搜索（找峰）阶段略逊（覆盖 0.899 < 0.979）——两者构成「搜索强覆盖 vs 报告精筛选」的互补格局。当维度升至 D=5，DSR-MO 覆盖显著退化（0.382），**不适用于 D≥5**，原因在于自估生态位半径受「维度灾难」影响失效（详见 §6）。

---

## 1. 定位与想解决的问题

多模态优化（MMO）的核心矛盾是**找得全**与**找得准**不可兼得：

- **找得全**：把决策空间里全部等高的全局峰盆地都发现出来（离散覆盖）。
- **找得准**：每个峰都精确压到盆底，且不多报冗余解（连续收敛 + 紧凑报告）。

传统做法在单一目标函数上同时硬压两者，往往顾此失彼。DSR-MO 的目标是**在不使用真峰位置（无先知）的前提下，同时做到高覆盖与高精度**。

> **🎯 定位：极低维度多模态优化算法（extremely low-dimensional MMO）**
>
> DSR-MO 专注 **D=2** 这一极低维度场景。「先占地盘、后精耕细作」依赖**自估生态位半径**（无先知），该机制只在低维下可靠；高维（D≥5）会因「维度灾难」使点距趋同、自估半径失效。因此 DSR-MO 把所有精力压在对 D=2 这一现实且高频场景的极致优化上，不追求不切实际的高维泛化。

## 2. 算法架构

DSR-MO 采用「搜索 + 精修 + 报告」三段式流水线，把一个目标拆成两个不同难度，分别交给合适机制完成：

```
初始化种群
   │  (随机采样)
   ▼
进化搜索（主循环）
   │  变异(SBX交叉 + 多项式变异) → 合并父代子代
   │  环境选择：fitness + maximin 最远点混合选择（保留等高峰）
   ▼
峰提取（每盆地一个代表）
   │  按目标排序 + 自估半径 clearing 去重
   ▼
局部精修（Nelder-Mead 无导数单纯形）
   │  D≥5 时 3× 随机扰动重启
   ▼
精修后函数值阈值过滤（剔除局部极小）
   ▼
报告期密度过滤（最终报告，无先知）
   │  简化密度聚类去冗余，每簇保留最优
   ▼
最终解集 X, F
```

**关键设计**：搜索期用 `maximin` 强制种群在决策空间铺开占位（保住**覆盖**），收敛精度完全交给后置的 Nelder-Mead 精修（保住**精度**）。两者解耦，互不拖累。

### 2.1 环境选择：fitness + maximin 混合

每次迭代把父代与子代合并后，从候选集选回种群规模 $n$ 个个体。先保留目标最优个体，之后每次选「目标越好 + 离已选集合越远」得分最高的个体：

$$
\text{score}(x) \;=\; -\,\text{normF}(x)\;+\;\lambda\,\frac{d_{\min}(x)}{\mathrm{dom}}
$$

其中：

- $\text{normF}(x) = \dfrac{F(x) - F_{\min}}{F_{\max} - F_{\min}}$ —— 归一化目标值 $\in[0,1]$，越小越好。
- $d_{\min}(x) = \min_{s\in S} \lVert x - s \rVert$ —— 个体到「已选集合 $S$」的最近距离。
- $\mathrm{dom} = \sqrt{\dim}\cdot(\mathrm{ub}-\mathrm{lb})$ —— 决策空间对角线，用于归一化距离。
- $\lambda$ —— 多样性权重（默认 20）。

### 2.2 生态位去重（clearing，无先知）

峰提取时按目标排序，用生态位半径把疏远点判定为新峰：

$$
d(p,\; x_r) > r \quad\Longrightarrow\quad p \ \text{is a new peak representative}
$$

其中 $x_r$ 为任一已有代表点。**自估半径**不依赖真峰半径 `problem.nichRad`：

$$
r \;=\; \mathop{\mathrm{median}}\limits_{i}\ \ \min_{j\neq i}\ \lVert x_i - x_j\rVert
$$

### 2.3 局部精修：Nelder-Mead 无导数单纯形

对每个候选峰代表，从初始单纯形迭代反射(Reflect)→扩展(Expand)→收缩(Contract)→压缩(Shrink)，沿弯曲谷道精确压到真全局峰。D≥5 时对每个代表做 3 次随机扰动重启，取最优。

### 2.4 报告期密度过滤（最终版）

对阈值过滤后的报告集做简化密度聚类（Union-Find 连通分量），每簇只保留目标最优的代表，消除「多报冗余」：

$$
\varepsilon \;=\; \mathop{\mathrm{median}}\limits_{i}\ \ \min_{j\neq i}\ \lVert \hat{x}_i - \hat{x}_j\rVert, \qquad
\hat{x}_k \;=\; \mathop{\mathrm{arg\,min}}\limits_{x\in C_k} F(x)
$$

这一模块不改变任何搜索核心代码，是正交的即插即用组件，是 Precision 从 0.756 → 0.992 的关键。

## 3. 与 CEC 2026 竞赛第一名的对比

DSR-MO 的定位，是在 **CEC 2026 多模态优化竞赛**这条最主流赛道上，正面挑战当年的**第一名**。我们不避讳这一点——比赛就得跟最强的人比。

> 为保持学术礼貌，这里不点名对手，仅以「CEC 2026 竞赛第一名」指代该赛事冠军算法（其论文：Chauhan & Dikshit，*S-CARD-CMSA: A Score-Aware Candidate Archive with Density-Filtered Reporting for Multimodal Optimization*, arXiv:2607.13764, 2026）。以下所有对比均在**尊重、公平、同口径**的前提下展开。

### 3.1 同环境实况复现对比（D=2，主结论）

为了公平，DSR-MO 在**同一台机器（MATLAB R2026a）、同一基准**下复现了第一名的开源实现，逐题逐实例（16 题 × 15 实例，预算 20,000×D）在 D=2 上真实跑了一遍，并用与 DSR-MO **完全相同**的评测口径（二分命中，相对容忍精度 1e-3）打分。复现实测数据（240 次独立运行，单次逐实例）：

| 指标 | 竞赛第一名（同环境复现，D=2） | **DSR-MO**（D=2，15 实例×5 次） |
|---|---|---|
| **Coverage / RPR** | **0.979** | 0.899 |
| **Precision** | 0.890 | **0.992** |
| **F1** | 0.916 | **0.935** |
| **官方得分 ½(RPR+F1)** | **0.947** | 0.917 |
| 成功覆盖率 SR | 0.742 | 0.538 |

**优势在哪**：DSR-MO 的**报告质量**更强——**Precision 0.992 与 F1 0.935 双双反超**（对 0.890 / 0.916）。DSR-MO 的密度过滤去冗余在「少报冗余、精准收口」上更极致，密度过滤后绝大多数问题 Precision=1.000。

**差距在哪**：第一名的**覆盖能力**更强——**Coverage 0.979** 明显高于 DSR-MO（0.899），这是它搜索引擎（协方差自适应演化策略）的优势，DSR-MO 坦率承认、也尊重。

**结论**：两者是典型的「搜索强覆盖 vs 报告精筛选」互补格局。DSR-MO 在报告（去冗余）阶段反超，在搜索（找峰）阶段略逊，但已如实呈现。

> **口径声明**：上表第一名数据为在 DSR-MO 作者的 MATLAB 环境（R2026a）下的逐实例单次复现，与 DSR-MO 用同一评测函数打分；DSR-MO 为跨 15 实例×5 次的均值。两者同为 D=2、20,000×D 预算，指标可直接对比。复现脚本见 `../复现_S-CARD-CMSA/run_reproduce_d2.m`。

**顺带提及**：其技术报告公布的官方数字（RPR 0.805 / Precision 0.915 / F1 0.831 / 官方分 0.818）为 768-run **跨维混合**均值，并非纯 D=2 全量，仅供了解其官方口径，不作为主结论依据——上面的同环境复现才是对比基准。

### 3.2 机制对比

| 维度 | CEC 2026 竞赛第一名 | DSR-MO |
|---|---|---|
| 搜索引擎 | 协方差自适应演化策略 + 禁忌区排斥 | 种群进化 + fitness+maximin 选择 + Nelder-Mead 精修 |
| 多样性保持 | 主动子种群 + 存档禁忌区排斥已找峰 | maximin（离已选集越远越好）铺开占位 |
| 收敛机制 | 协方差矩阵自适应采样 | 无导数 Nelder-Mead 单纯形 |
| 报告阶段 | 被动二级存档 + 得分感知密度过滤 | 阈值过滤 + 简化密度聚类去冗余（无先知） |
| 使用真峰位置 | 否 | 否 |
| 核心哲学 | 搜索期原样 + 报告期精细筛选 | 搜索期强覆盖 + 报告期去冗余，两阶段都主动优化 |

**关键差异**：第一名把平衡 RPR 与 F1 的功夫全放在**报告期**（密度过滤、少报冗余），搜索期保持原样；DSR-MO 把核心功夫放在**搜索期**（maximin 强制铺开 + 精修压实）。DSR-MO 最终版把两者结合：搜索期强覆盖 + 报告期简化密度去冗余，Precision 由 ~0.75 抬到 0.992 而 Coverage 无损——证明第一名的报告期去冗余思想可无缝嫁接到 DSR-MO 上，形成「搜索强 + 报告精」的完全体。

## 4. 实测成绩

评测口径：**MATLAB R2026a，黑盒（官方预算 20,000×D + 无真峰先知半径），DSR-MO 最终版（含报告期密度过滤）**，D=2 全 16 题 × 15 实例 × 5 次。

### 4.1 D=2 全量汇总

| 指标 | 数值 |
|---|---|
| **Coverage（找峰率 / RPR）** | **0.8993** |
| **Precision（精度）** | **0.9921** |
| **F1** | **0.9345** |
| **SR（完全找齐率）** | 0.5383 |
| **官方得分 ½(RPR+F1)** | **0.9169** |

### 4.2 D=2 逐问题结果（15 实例 × 5 次平均）

| PID | 峰数 | Coverage | Precision | F1 | PID | 峰数 | Coverage | Precision | F1 |
|---|---|---|---|---|---|---|---|---|---|
| P01 | 20 | 0.966 | **1.000** | 0.982 | P09 | 10 | 0.960 | **1.000** | 0.979 |
| P02 | 20 | 0.987 | **1.000** | 0.993 | P10 | 10 | 0.997 | **1.000** | 0.999 |
| P03 | 20 | 0.971 | **1.000** | 0.985 | P11 | 10 | 0.993 | **1.000** | 0.996 |
| P04 | 20 | 0.954 | **1.000** | 0.976 | P12 | 10 | 0.979 | **1.000** | 0.989 |
| P05 | 20 | 0.995 | **1.000** | 0.998 | P13 | 10 | **1.000** | **1.000** | **1.000** |
| P06 | 20 | 0.617 | **1.000** | 0.758 | P14 | 10 | 0.532 | **1.000** | 0.684 |
| P07 | 20 | 0.872 | 0.942 | 0.904 | P15 | 10 | 0.612 | 0.932 | 0.734 |
| P08 | 20 | 0.975 | **1.000** | 0.987 | P16 | 10 | 0.977 | **1.000** | 0.988 |

- **找峰极强**：P05/P10/P11/P12/P13 覆盖 >0.99，多数达 1.000。
- **精度上限**：密度过滤后绝大多数问题 Precision=1.000，仅 P07(~0.94)/P15(~0.93) 略低。
- **最弱**：P14(0.532)/P06(0.617)/P15(0.612)——欺骗性最强、峰最密集的问题，自估半径在此受限，但即便覆盖受限其 Precision 也≈1.000。

## 5. 遇到的问题与解决

开发与评测过程中，DSR-MO 遇到并逐一解决了以下问题。

### 5.1 移植 bug（基准正确性）

| 问题 | 表现 | 解决 |
|---|---|---|
| **Weierstrass 漏平移项** | 漏了 `+0.5`，P07/P15 在真峰处求值偏差 1.8~5.2 | 补平移项，16 问题基准自检全部通过（maxDev=0） |
| **sequences.csv 索引越界** | 官方数据为 1-based，多加了 `+1`，D=5 时 `numUniform(10001)` 越界 | 修正索引，D=5 恢复可运行 |

### 5.2 算法设计问题

| 问题 | 表现 | 解决 |
|---|---|---|
| **精修后缺函数值过滤** | Ackley 返回大量局部极小候选，Precision 偏低 | 新增阈值过滤，Precision 显著提升 |
| **预算超标 + 先知半径** | 早期用 50000×dim 预算、真峰半径，不可对标官方 | 改为官方预算 20000×dim + 自估半径（无 Oracle） |
| **Nelder-Mead 高维敏感** | 对初始单纯形敏感，D≥5 收敛不稳 | D≥5 对每个代表做 3 次随机扰动重启，取最优 |
| **报告冗余拖低精度** | 同一盆地重复报告，Precision 仅 0.756 | 新增报告期密度过滤（`dsr_mo_density_filter.m`），Precision 提到 0.992，Coverage 无损 |

### 5.3 方法论洞察

对照实验（隔离 λ、预算、先知）揭示一条与直觉相反的经验：**在多模态搜索前期，个体目标值好坏远不如「是否离已探索区域足够远」重要**。λ=20 强制种群铺开占位，哪怕早期目标值很差，也能在搜索后期用无导数 Nelder-Mead 精修把收敛损失完全弥补回来。这就是「先占地盘、后精耕细作」成立的原因。

## 6. D=5 能力边界（实测中间过程）

为完整评估适用维度范围，DSR-MO 在 D=5 下也做了实测（与 D=2 相同官方口径：16 题 × 15 实例 × 预算 20,000×D）。**实测过程**如实记录如下。

### 6.1 实测进度与汇总

截至统计节点，D=5 已运行 **201 / 240** 个（问题×实例），覆盖全部 16 个问题。已完成的 201 次汇总：

| 指标 | D=5 实测（201 次） |
|---|---|
| **Coverage（找峰率）** | **0.382** |
| **Precision（精度）** | 0.870 |
| **F1** | 0.486 |
| **官方得分 ½(RPR+F1)** | **0.434** |

### 6.2 分布特征（中间数据）

- **最优单次**：Coverage 最高 0.900（仅个别实例），次优 0.850、0.800 若干。
- **最差单次**：存在多个实例 Coverage = 0.000（一个峰都没找到）。

### 6.3 结论：DSR-MO 不适用于 D=5

1. **覆盖崩坏**：Coverage 仅 0.382，20 个峰平均只能找到约 7~8 个，一半以上峰漏掉。Precision 尚可（0.870），但「找到的峰太少」直接把 F1 与官方分拖到 0.43 量级。
2. **机制根源——维度灾难**：DSR-MO 的自估生态位半径依赖候选解之间的**最近邻距离中位数**。当维度升高（D≥5），高维空间点对距离趋于一致，最近邻距离分布被压缩，自估半径失真，导致生态位过度聚合、覆盖退化。这是「先占地盘」策略在高维下的固有局限，而非参数调优可解决。
3. **因此**：DSR-MO **明确限位在 D=2 极低维度**，不宣称高维泛化能力。D=5 及以上的评测结果**不作为 DSR-MO 的性能基准**，仅用于展示其适用边界。

## 7. 复现方法与文件清单

### 7.1 复现方法

```matlab
% MATLAB R2026a，官方规则：D=2 × 15 实例 × 16 题 × 5 次，黑盒（官方预算 + 无先知）
% 最终版：报告期密度过滤默认开启（report_density=true），预算默认 20000×dim
matlab -batch "addpath(pwd); addpath(fullfile(pwd,'cec2026')); res=run_dsr_mo([2],1:15,1:16,5,struct('pop_size',300,'lambda',20)); save(fullfile(pwd,'results_official.mat'),'res');"
```

### 7.2 文件清单

| 文件 | 作用 |
|---|---|
| `dsr_mo.m` | 主算法（含报告期密度过滤） |
| `dsr_mo_density_filter.m` | 报告期密度过滤模块 |
| `dsr_mo_nelder_mead.m` | 局部精修（Nelder-Mead） |
| `dsr_mo_metrics.m` | 评测指标（RPR / Precision / F1 / Coverage / SR） |
| `run_dsr_mo.m` | 主运行入口 |
| `make_figures.m` | 一键生成全部论文图（输出到 `figs/`） |
| `figs/` | 6 张评测图 |
| `cec2026/` | CEC 2026 基准套件（`ProblemMM.m` / `BasicFun.m` / `UtilityMethod.m` / `pdist2.m` / `data/`） |
| `README.md` / `README_EN.md` | 简化说明（中 / 英） |

---

*生成环境：MATLAB R2026a；基准：CEC 2026 多模态优化基准；对比对象：CEC 2026 多模态优化竞赛第一名（为保持学术礼貌，不点名）。*